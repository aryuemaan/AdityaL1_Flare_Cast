# Core runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# For optional back-ends/interfaces install the extras, e.g.:
#   pip install -e ".[serve,dashboard,boost]"
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
pydantic>=2.5
pydantic-settings>=2.1
typer>=0.12
sqlalchemy>=2.0
pyarrow>=14.0
